# Contributing

Issues and pull requests are welcome. Keep changes focused and include tests for changed behavior.

```sh
npm ci
npm test
node ./bin/mcp-config-audit.js --require-config ./examples
```

The project intentionally has no runtime dependencies. Please discuss any new runtime dependency before opening a pull request.
